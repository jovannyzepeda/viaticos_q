<%= form_for([@zone,@proyect,@viatico]) do |f| %>
  <% if @viatico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@viatico.errors.count, "error") %> prohibited this viatico from being saved:</h2>

      <ul>
      <% @viatico.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :cantidad , "Cantidad solicitada"%><br>
    <%= f.text_field :cantidad , required: "required", class: "form-control", placeholder: "Ej. 123.98"%>
  </div>
  <div class="form-group date">
    <%= f.label :fecha , "Fecha de solicitud" %><br>
    <%= f.date_field :fecha %>
  </div>
  <div class="actions">
    <%= f.submit  "Guardar" , class: 'btn btn-primary btn-sm guardar'  %>
  </div>
<% end %>
